#include "main.h"

/**
 * _strlen - This returns the length of a string
 * @s: string to find the length 
 *
 * Return: length of s(string)
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	return (i);
}
